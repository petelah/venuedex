from vd import create_app

app = create_app()

if __name__ == '__main__':
    app.run(debug=True)

    # region, business_name, "desc", menu, food, \
    # booze, pickup, selfd, uber,
    # deliveroo, instagram, facebook, email, spinfo, address, phone
